# Copy to .env.local and fill in once Stage 4/5 (real AI + persistence) begins.
# Nothing in this file is required to run the Stage 3 mocked prototype (npm run dev).

# --- Supabase (Stage 5) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- OpenAI (Stage 4) ---
# Server-side only. Never exposed to the client.
OPENAI_API_KEY=

# --- Stripe (Stage 9, pilot pricing test) ---
STRIPE_SECRET_KEY=
STRIPE_PAYMENT_LINK_URL=
